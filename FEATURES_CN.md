# Graded Reader GenAI 功能更新说明

本项目近期完成了 6 项核心功能的升级与优化。以下是具体的功能说明与技术实现细节：

## 1. 语法与词形变化分级控制 (Grammar Progression & Word Morphology)
- **目标**：让生成的阅读文章不仅在**词汇**上符合等级，在**语法**上也符合对应等级的水平。
- **实现细节**：
  - 更新了大语言模型（LLM）的系统 Prompt，根据不同等级动态应用语法规则（例如：Level 1-3 强制使用简单句和一般现在时/过去时，严禁复杂从句）。
  - 明确允许 AI 使用所选高频词汇的自然形态变化（如复数 `-s`，过去式 `-ed`，进行时 `-ing`），使生成的文章更加自然流畅。

## 2. 阅读理解互动测试 (Reading Comprehension Quiz)
- **目标**：在文章阅读后增加互动测试，巩固学习者的理解能力。
- **实现细节**：
  - AI 生成故事时，会根据文章内容自动出 2-3 道英语阅读理解单选题。
  - 前端新增了 `QuizViewer` 组件，用户在阅读完文章后可直接在页面上答题，并实时验证正确率与查看正确答案。

## 3. 剥离 Prompt 标签与正则动态高亮 (Decouple Tagging from Prompt)
- **目标**：防止 AI 为了给特定单词加括号 `[word]` 或 `{word}` 而破坏英语句子的自然连贯性和标点符号。
- **实现细节**：
  - 移除了 Prompt 中要求 AI 使用特殊符号标记单词的指令，要求 AI 仅输出纯净、自然的英文故事。
  - 在前端 `StoryRenderer` 组件中，直接提取接口返回的目标词汇表 (`targetWordsUsed` 和 `outOfScopeWords`)，动态构建正则表达式，在页面渲染时对重点词汇进行精准的 Regex 匹配并高亮展示。

## 4. 严谨的 JSON 数据结构验证 (Robust JSON Validation)
- **目标**：增强系统的健壮性与稳定性，防止大模型产生的“幻觉”（如漏传字段、格式错误）导致前端白屏或崩溃。
- **实现细节**：
  - 引入了 TypeScript 生态中标准的 `zod` 数据校验库。
  - 为 AI 返回的结构化数据（如标题、正文、单词本、测验题等）制定了严格的 Schema 校验。一旦数据格式不符，会在服务层被捕获并抛出清晰的验证错误提示。

## 5. 原生 TTS 语音朗读功能 (Text-to-Speech Integration)
- **目标**：实现“视听结合”，为英语学习者提供听力训练。
- **实现细节**：
  - 完美集成了浏览器原生的 `window.speechSynthesis` API。
  - 在文章阅读器中加入了“Read Aloud（朗读）”和“Stop Reading（停止）”按钮。为了适配语言学习者，语音播放的语速默认设定为更易听懂的 `0.9` 倍速。

## 6. 动态字数与篇幅控制 (Target Length Control)
- **目标**：根据用户的学习阶段控制文章长度，避免初学者看到过长文章而产生挫败感，同时保证高级别用户的阅读量。
- **实现细节**：
  - 在向大模型发送请求时，通过公式动态计算目标字数（例如 Level 1 目标为 65 词左右，随等级递增，最高限制在 400 词），并在 Prompt 中约束 AI 遵循该篇幅生成。
